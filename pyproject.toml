[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "taskflow-git-demo"
version = "0.0.0"
description = "A Git workflow learning project"
requires-python = ">=3.10"

[project.scripts]
taskflow = "taskflow.cli:main"
